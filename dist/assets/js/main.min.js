(function(){const body=document.body;const $=(selector,scope=document)=>scope.querySelector(selector);const $$=(selector,scope=document)=>Array.from(scope.querySelectorAll(selector));const prefersReducedMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const clamp=(value,min,max)=>Math.min(Math.max(value,min),max);const SPOILER_STORAGE_KEY="wizardSpoilersVisible";const closeMobileNav=()=>{body.classList.remove("nav-open");const navToggle=$("[data-nav-toggle]");const navMenu=$("[data-nav-menu]");if(navToggle){navToggle.setAttribute("aria-expanded","false");navToggle.focus();}
if(navMenu){navMenu.removeAttribute("data-open");}};const initNavigation=()=>{const navToggle=$("[data-nav-toggle]");const navMenu=$("[data-nav-menu]");if(!navToggle||!navMenu)return;navToggle.addEventListener("click",()=>{const expanded=navToggle.getAttribute("aria-expanded")==="true";navToggle.setAttribute("aria-expanded",String(!expanded));body.classList.toggle("nav-open",!expanded);if(!expanded){navMenu.setAttribute("data-open","true");}else{navMenu.removeAttribute("data-open");}});navMenu.addEventListener("click",(event)=>{if(event.target.closest("a")){closeMobileNav();}});window.addEventListener("keyup",(event)=>{if(event.key==="Escape"&&body.classList.contains("nav-open")){closeMobileNav();}});};const populateTraitOptions=(select,houses)=>{if(!select)return;const traits=new Set();houses.forEach((house)=>house.traits.forEach((trait)=>traits.add(trait)));const fragment=document.createDocumentFragment();traits.forEach((trait)=>{const option=document.createElement("option");option.value=trait;option.textContent=trait;fragment.appendChild(option);});select.appendChild(fragment);};const populateWizardHouseOptions=(select,houses)=>{if(!select)return;const fragment=document.createDocumentFragment();houses.forEach((house)=>{const option=document.createElement("option");option.value=house.id;option.textContent=house.name;fragment.appendChild(option);});select.appendChild(fragment);};const populateWizardYearOptions=(select,wizards)=>{if(!select)return;const years=new Set();wizards.forEach((wizard)=>wizard.years.forEach((year)=>years.add(year)));const sortedYears=Array.from(years).sort((a,b)=>a-b);const fragment=document.createDocumentFragment();sortedYears.forEach((year)=>{const option=document.createElement("option");option.value=String(year);option.textContent=String(year);fragment.appendChild(option);});select.appendChild(fragment);};const renderHouseCard=(house)=>{const article=document.createElement("article");article.className="card card--house";article.innerHTML=`
      <figure class="card__media">
        <img src="${house.img}" alt="${house.name} house crest" loading="lazy" width="320" height="320" />
        <span class="card__label" aria-hidden="true">House</span>
      </figure>
      <div class="card__body">
        <h2 class="card__title">${house.name}</h2>
        <p class="card__summary">${house.summary}</p>
        <dl class="card__meta">
          <div>
            <dt>Founder</dt>
            <dd>${house.founder}</dd>
          </div>
          <div>
            <dt>Mascot</dt>
            <dd>${house.mascot}</dd>
          </div>
          <div>
            <dt>Relic</dt>
            <dd>${house.relic}</dd>
          </div>
          <div>
            <dt>Ghost</dt>
            <dd>${house.ghost}</dd>
          </div>
        </dl>
        <ul class="card__tags" aria-label="Traits">
          ${house.traits
            .map(
              (trait) =>
                `<li><span class="badge"data-badge="${trait.toLowerCase()}">${trait}</span></li>`
            )
            .join("")}
        </ul>
      </div>
    `;return article;};const renderWizardCard=(wizard,houses)=>{const article=document.createElement("article");article.className="card card--wizard";article.dataset.spoiler=wizard.spoilerLevel;const house=houses.find((entry)=>entry.id===wizard.house)||{name:wizard.house,colors:[],};const yearsLabel=wizard.years.length?`${Math.min(...wizard.years)}–${Math.max(...wizard.years)}`:"Unknown years";article.innerHTML=`
      <figure class="card__media">
        <img src="${wizard.img}" alt="${wizard.name} portrait" loading="lazy" width="320" height="440" />
        <figcaption class="visually-hidden">${wizard.name}</figcaption>
      </figure>
      <div class="card__body">
        <div class="card__heading">
          <h2 class="card__title">${wizard.name}</h2>
          <span class="badge badge--outline" data-house="${wizard.house}">${house.name}</span>
        </div>
        <p class="card__summary">${wizard.summary}</p>
        <dl class="card__meta">
          <div>
            <dt>Aliases</dt>
            <dd>${wizard.aliases.join(", ")}</dd>
          </div>
          <div>
            <dt>Years at Hogwarts</dt>
            <dd>${yearsLabel}</dd>
          </div>
        </dl>
      </div>
    `;return article;};const renderEmptyState=(container,message)=>{container.innerHTML=`
      <div class="empty-state" role="status">
        <p>${message}</p>
      </div>
    `;};const initFeaturedSection=()=>{const section=$("[data-featured]");if(!section||!window.HOUSES||!window.WIZARDS)return;const house=window.HOUSES[0];const wizard=window.WIZARDS.find((entry)=>entry.spoilerLevel==="low");if(!house||!wizard)return;section.innerHTML=`
      <div class="featured__item">
        <h2>Featured House</h2>
        <article class="card card--featured">
          <figure class="card__media">
            <img src="${house.img}" alt="${house.name} crest" loading="lazy" width="320" height="320" />
          </figure>
          <div class="card__body">
            <h3 class="card__title">${house.name}</h3>
            <p class="card__summary">${house.summary}</p>
            <a class="button button--ghost" href="houses.html#${house.id}">
              Explore ${house.name}
            </a>
          </div>
        </article>
      </div>
      <div class="featured__item">
        <h2>Featured Wizard</h2>
        <article class="card card--featured">
          <figure class="card__media">
            <img src="${wizard.img}" alt="${wizard.name} portrait" loading="lazy" width="320" height="440" />
          </figure>
          <div class="card__body">
            <h3 class="card__title">${wizard.name}</h3>
            <p class="card__summary">${wizard.summary}</p>
            <a class="button button--ghost" href="wizards.html#${wizard.id}">
              Meet ${wizard.name}
            </a>
          </div>
        </article>
      </div>
    `;};const initHousesPage=()=>{if(!body.classList.contains("page-houses")||!window.HOUSES)return;const form=$("[data-houses-form]");const searchInput=$("[data-house-search]");const traitSelect=$("[data-house-trait]");const grid=$("[data-houses-grid]");const count=$("[data-house-count]");if(!grid||!form||!searchInput||!traitSelect)return;populateTraitOptions(traitSelect,window.HOUSES);const render=()=>{grid.setAttribute("aria-busy","true");const query=searchInput.value.trim().toLowerCase();const trait=traitSelect.value;const filtered=window.HOUSES.filter((house)=>{const matchesName=house.name.toLowerCase().includes(query);const matchesTrait=!trait||house.traits.some((item)=>item===trait);return matchesName&&matchesTrait;});grid.innerHTML="";if(!filtered.length){renderEmptyState(grid,"No houses match those filters yet. Try a different search.");}else{const fragment=document.createDocumentFragment();filtered.forEach((house)=>{const card=renderHouseCard(house);card.id=house.id;fragment.appendChild(card);});grid.appendChild(fragment);}
if(count){count.textContent=`${filtered.length} ${filtered.length === 1 ? "house" : "houses"} found`;}
grid.setAttribute("aria-busy","false");};form.addEventListener("input",render);form.addEventListener("submit",(event)=>event.preventDefault());render();};const initWizardsPage=()=>{if(!body.classList.contains("page-wizards")||!window.WIZARDS)return;const searchInput=$("[data-wizard-search]");const houseSelect=$("[data-wizard-house]");const yearSelect=$("[data-wizard-year]");const spoilerToggle=$("[data-spoiler-toggle]");const grid=$("[data-wizards-grid]");const count=$("[data-wizard-count]");if(!grid||!searchInput||!houseSelect||!yearSelect||!spoilerToggle)
return;populateWizardHouseOptions(houseSelect,window.HOUSES||[]);populateWizardYearOptions(yearSelect,window.WIZARDS);const render=()=>{grid.setAttribute("aria-busy","true");const query=searchInput.value.trim().toLowerCase();const house=houseSelect.value;const year=yearSelect.value;const filtered=window.WIZARDS.filter((wizard)=>{const matchesName=wizard.name.toLowerCase().includes(query);const matchesHouse=!house||wizard.house===house;const matchesYear=!year||wizard.years.some((entry)=>String(entry)===year);return matchesName&&matchesHouse&&matchesYear;});grid.innerHTML="";if(!filtered.length){renderEmptyState(grid,"No wizards match those filters yet. Try broadening your criteria.");}else{const fragment=document.createDocumentFragment();filtered.forEach((wizard)=>{const card=renderWizardCard(wizard,window.HOUSES||[]);card.id=wizard.id;fragment.appendChild(card);});grid.appendChild(fragment);}
if(count){count.textContent=`${filtered.length} ${filtered.length === 1 ? "wizard" : "wizards"} found`;}
grid.setAttribute("aria-busy","false");};const readStoredSpoilerPreference=()=>{try{return localStorage.getItem(SPOILER_STORAGE_KEY);}catch(error){return null;}};const writeStoredSpoilerPreference=(state)=>{try{localStorage.setItem(SPOILER_STORAGE_KEY,state);}catch(error){}};const updateSpoilerState=(persist=true)=>{const hideSpoilers=!spoilerToggle.checked;body.classList.toggle("spoiler-hidden",hideSpoilers);if(persist){writeStoredSpoilerPreference(hideSpoilers?"hidden":"visible");}};searchInput.addEventListener("input",render);houseSelect.addEventListener("change",render);yearSelect.addEventListener("change",render);spoilerToggle.addEventListener("change",()=>{updateSpoilerState();if(!prefersReducedMotion){body.classList.add("spoiler-animate");window.setTimeout(()=>body.classList.remove("spoiler-animate"),600);}});render();const storedPreference=readStoredSpoilerPreference();if(storedPreference==="hidden"){spoilerToggle.checked=false;}else if(storedPreference==="visible"){spoilerToggle.checked=true;}
updateSpoilerState(false);};const buildTimelineEvents=()=>{const events=[];if(Array.isArray(window.HOUSES)){window.HOUSES.forEach((house)=>{(house.milestones||[]).forEach((event)=>{events.push({...event,source:house.name,type:"house",});});});}
if(Array.isArray(window.WIZARDS)){window.WIZARDS.forEach((wizard)=>{(wizard.notableEvents||[]).forEach((event)=>{events.push({...event,source:wizard.name,type:"wizard",});});});}
return events.sort((a,b)=>a.year-b.year);};const renderTimeline=(container,events)=>{const grouped=events.reduce((acc,event)=>{const year=String(event.year);if(!acc.has(year)){acc.set(year,[]);}
acc.get(year).push(event);return acc;},new Map());container.innerHTML="";const fragment=document.createDocumentFragment();grouped.forEach((items,year)=>{const section=document.createElement("section");section.className="timeline__year";const toggleId=`timeline-${year}`;section.innerHTML=`
        <div class="timeline__year-header">
          <h2 id="${toggleId}">${year}</h2>
          <button
            type="button"
            class="timeline__toggle"
            aria-expanded="true"
            aria-controls="${toggleId}-panel"
          >
            Collapse
          </button>
        </div>
        <div
          class="timeline__panel"
          id="${toggleId}-panel"
          role="region"
          aria-labelledby="${toggleId}"
        >
          <ol class="timeline__list">
            ${items
              .map(
                (item) => `<li class="timeline__item timeline__item--${item.type}"><div class="timeline__marker"aria-hidden="true"></div><div class="timeline__content"><p class="timeline__source">${item.type==="house"?"House":"Wizard"}· ${item.source}</p><h3>${item.title}</h3><p>${item.description}</p></div></li>`
              )
              .join("")}
          </ol>
        </div>
      `;fragment.appendChild(section);});container.appendChild(fragment);container.addEventListener("click",(event)=>{const button=event.target.closest(".timeline__toggle");if(!button)return;const expanded=button.getAttribute("aria-expanded")==="true";const panel=button.closest(".timeline__year").querySelector(".timeline__panel");button.setAttribute("aria-expanded",String(!expanded));button.textContent=expanded?"Expand":"Collapse";panel.hidden=expanded;});};const initTimelinePage=()=>{if(!body.classList.contains("page-timeline"))return;const container=$("[data-timeline]");if(!container)return;const events=buildTimelineEvents();if(!events.length){renderEmptyState(container,"Timeline data is loading slowly. Please try again shortly.");return;}
container.setAttribute("aria-busy","true");renderTimeline(container,events);container.setAttribute("aria-busy","false");};const activateScrollEffects=()=>{if(prefersReducedMotion)return;const hero=$(".hero");if(!hero)return;window.addEventListener("scroll",()=>{const y=clamp(window.scrollY/600,0,1);hero.style.setProperty("--hero-vignette-opacity",String(0.25+y*0.35));},{passive:true});};initNavigation();initFeaturedSection();initHousesPage();initWizardsPage();initTimelinePage();activateScrollEffects();})();